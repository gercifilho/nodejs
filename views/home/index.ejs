<html>
    <body>
    <h2 class="basic-title">Livraria</h2>
    <table class="table table-condensed table-bordered
    table-striped table-hover">
        <thead>
            <tr>
                <td>Titulo</td>
                <td>Preco</td>
                <td>Descricao</td>
            </tr>
        </thead>
        <tbody>
            <% for(var i=0; i<lista.length; i++) {%>
            <tr>
                <td><%= lista[i].titulo %></td>
                <td><%= lista[i].preco %></td>
                <td><%= lista[i].descricao %></td>
            </tr>
            <% } %>
        </tbody>
    </table>
     <script src="http://localhost:3001/socket.io/socket.io.js"></script> <!-- This Creates  io -->
     <script>
         var socket = io();
         socket.on('novaPromocao', function (data) {
            console.log("Nova Promoção");
            console.log(data);
         });
     </script>
    </body>
</html>